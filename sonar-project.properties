sonar.projectKey=pkic_self-assessment
sonar.organization=pkic

# Frozen snapshot of the released 1.0.0 model's category/requirement names.
# Every entry is structurally identical by design — the file is a data map,
# not code — so the duplication detector flags ~40 copy-paste blocks that
# we can't (and shouldn't) deduplicate without losing the literal mapping.
sonar.cpd.exclusions=src/legacy/pkimm-model-1.0.0-names.ts
