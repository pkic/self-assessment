import './SelfAssessmentComponent';
import './index.module.scss';
import React from 'react';
import { createRoot } from 'react-dom/client';

// No need to render anything directly here if using the custom element directly in HTML
